# Example environment variables for URL Shortener
# Copy this file to `.env.local` and fill in the actual values before running the app.

# Instance Configuration (White-Label Settings)
# These values override the defaults in config.ts
NEXT_PUBLIC_APP_NAME="LinkSnip"           # Your branded app name
NEXT_PUBLIC_APP_DOMAIN="linksnip.com"     # Your domain without protocol
NEXT_PUBLIC_APP_URL="https://linksnip.com" # Full URL with protocol

# Database Configuration (Required)
# Format: mongodb+srv://<USERNAME>:<PASSWORD>@<CLUSTER_URL>/<DATABASE_NAME>?retryWrites=true&w=majority
MONGODB_URI="mongodb+srv://<USERNAME>:<PASSWORD>@cluster0.mongodb.net/your-db?retryWrites=true&w=majority"

# Redis Configuration (Required for rate limiting and caching)
UPSTASH_REDIS_REST_URL="your-upstash-url"        # Upstash Redis REST API URL
UPSTASH_REDIS_REST_TOKEN="your-upstash-token"    # Upstash Redis REST API Token

# Security Settings
RATE_LIMIT_REQUESTS=10          # Maximum requests per window
RATE_LIMIT_WINDOW_MS=60000     # Window size in milliseconds (60 seconds)
SHORT_URL_LENGTH=6             # Length of generated short URLs


# Feature Flags
# Use these to enable/disable features in different environments
ENABLE_ANALYTICS=true          # Enable click tracking and analytics
ENABLE_ADS=true               # Enable ad-supported redirects
ENABLE_CUSTOM_DOMAINS=false    # Enable custom domain support

# Cloudflare Analytics Token
# Set this to your Cloudflare Web Analytics token to enable analytics on all user-facing pages
NEXT_PUBLIC_CLOUDFLARE_ANALYTICS_TOKEN=

# Contact Information
SUPPORT_EMAIL="support@yourdomain.com"

# The config.ts file contains defaults for all these settings.
# Use environment variables to override values for different:
# - Environments (development, staging, production)
# - Branded instances
# - Security configurations
