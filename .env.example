# Example environment variables for Free URL Shortener
# Copy this file to `.env.local` and fill in the actual values before running the app.

# MongoDB connection string (replace placeholders with your credentials and cluster info)
# Format: mongodb+srv://<USERNAME>:<PASSWORD>@<CLUSTER_URL>/<DATABASE_NAME>?retryWrites=true&w=majority
MONGODB_URI="mongodb+srv://<USERNAME>:<PASSWORD>@cluster0.mongodb.net/your-db?retryWrites=true&w=majority"

# (Optional) Base URL override if running on a custom domain or port
# BASE_URL="http://localhost:3000"

# Domain name used to prevent recursive URL shortening (required)
# This should be your service's domain name without protocol (e.g., "short.ly" or "localhost:3000")
DOMAIN="your-domain.com"

# Add other secrets or keys here as needed for your environment
# e.g. NEXT_PUBLIC_ANALYTICS_ID="your-analytics-id"
